@{
    ViewData["Title"] = "Consulta de Orden por ID";
}

<style>
    .consulta-wrapper {
        min-height: 70vh;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .consulta-card {
        max-width: 650px;
        width: 100%;
        border-radius: 1rem;
        box-shadow: 0 0.5rem 1.25rem rgba(0,0,0,.08);
        border: 1px solid #eef1f5;
    }

    .consulta-badge {
        font-size: .85rem;
        text-transform: uppercase;
        letter-spacing: .08em;
    }

    #resultadoOrden table th {
        width: 30%;
        background-color: #f8f9fa;
    }
</style>

<div class="container consulta-wrapper">
    <div class="card consulta-card">
        <div class="card-body">

            <div class="d-flex justify-content-between align-items-center mb-3">
                <div>
                    <span class="badge bg-primary-subtle text-primary consulta-badge">
                        Consulta rápida
                    </span>
                    <h2 class="h4 mt-2 mb-0">@ViewData["Title"]</h2>
                    <p class="text-muted mb-0">
                        Digite el ID de la orden para ver su información detallada.
                    </p>
                </div>
                <div class="text-end">
                    <a href="/Ordenes/Index" class="btn btn-outline-secondary btn-sm">
                        ← Volver al listado
                    </a>
                </div>
            </div>

            <hr />

            <div class="input-group mb-3">
                <span class="input-group-text">
                    ID
                </span>
                <input type="number" id="buscarIdOrden" class="form-control" placeholder="Ingrese el ID de la orden">
                <button class="btn btn-primary" id="btnBuscarOrden">Buscar</button>
            </div>

            <small class="text-muted d-block mb-2">
                Ejemplo: 1, 2, 3...
            </small>

            <div id="resultadoOrden" style="display:none;">
                <h5 class="mt-3 mb-2">Resultado</h5>
                <div class="table-responsive">
                    <table class="table table-bordered align-middle mb-0">
                        <tbody>
                            <tr><th>ID</th><td id="resIdOrden"></td></tr>
                            <tr><th>Producto</th><td id="resProductoIdOrden"></td></tr>
                            <tr><th>Cantidad</th><td id="resCantidadOrden"></td></tr>
                            <tr><th>Fecha</th><td id="resFechaOrden"></td></tr>
                            <tr><th>Estado</th><td id="resEstadoOrden"></td></tr>
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/Ordenes.js" asp-append-version="true"></script>
}
